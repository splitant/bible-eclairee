/**
 * @file
 * Be Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.beTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
